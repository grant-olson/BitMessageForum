%h2 Identities

%h3 Create random address

%div
  %form{:method => :post, :action => "/identities/new/"}
    %label
      Label:
      %input.form-control{:type => :text, :name => :label, :size => 50}
    %input.btn{:type => :submit, :value => 'Create!' }

%h3 Create deterministic address

%div
  %form{:method => :post, :action => "/identities/new/"}
    %label
      Label:
      %input.form-control{:type => :text, :name => :passphrase, :size => 50}
    %input.btn{:type => :submit, :value => 'Create!' }


%h3 Current Identities
%table.table.table-striped
  %tr
    %th Label
    %th Address
  - @addresses.each_pair do |address, address_info|
    - if address_info['enabled']
      %tr
        %th= address_info["label"]
        %td= address

